<!--
Copy this file to docs/blueprints/components/<component-name>.md for each
architecturally-significant component. Keep it current as changes land —
concise, pointing at real code paths rather than restating implementation
detail. See docs/blueprints/architecture.md for how components relate,
and CLAUDE.md for when this must be updated.
-->

# Component Blueprint: `<path/to/component>`

## Purpose

What this component is responsible for, today — link to the design doc(s)
that most recently changed it, and to the
[architecture blueprint](../architecture.md) for how it fits the whole.

## Key files

- [`<path>`](../../../<path>) — what it does and why it's structured
  that way (only note the non-obvious)

## Notes

<Anything a reader needs to safely change this component: invariants,
gotchas, deliberate non-obvious decisions. Skip this section if there's
nothing worth flagging.>
