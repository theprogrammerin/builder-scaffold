# Feature Blueprint

Snapshot of what this project can actually do today, described from the
user's point of view. For where it's headed, see
[`docs/backlog.md`](../backlog.md); for how each piece works
internally, see the per-component docs under
[`components/`](./components/).

No roadmap or plan content belongs here — only capabilities that exist
right now. Update this file in the same turn as any change that adds or
alters a capability (see the Changelog rule in [`CLAUDE.md`](../../CLAUDE.md)).

## <Capability area>

<One or two sentences describing what a user can do, in plain terms —
not implementation detail.>

- Details: [component blueprint](./components/<component-name>.md)
